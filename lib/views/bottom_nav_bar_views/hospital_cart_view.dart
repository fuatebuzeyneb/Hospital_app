import 'package:flutter/material.dart';

class HospitalCartView extends StatelessWidget {
  const HospitalCartView({super.key});

  static String id = 'HospitalCartView';

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      backgroundColor: Colors.white,
      body: Center(),
    );
  }
}
